#ifndef DEVNET_H    /* This is an "include guard" */
#include <time.h>

#define DEVNET_H    /* prevents the file from being included twice. */
                     /* Including a header file twice causes all kinds */
                     /* of interesting problems.*/

void startNET(int port);

#endif /* DEVNET_H */