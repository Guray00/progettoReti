#ifndef SIGNUP_H    /* This is an "include guard" */
#define SIGNUP_H    /* prevents the file from being included twice. */
                     /* Including a header file twice causes all kinds */
                     /* of interesting problems.*/

int signup();
int get_device_port(char* username);


#endif /* SIGNUP_H */