#ifndef REGISTER_H    /* This is an "include guard" */
#include <time.h>

#define REGISTER_H    /* prevents the file from being included twice. */
                     /* Including a header file twice causes all kinds */
                     /* of interesting problems.*/

int update_register_login(char* username);

#endif /* REGISTER_H */